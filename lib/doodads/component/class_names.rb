# frozen_string_literal: true

module Doodads
  class Component
    module ClassNames
      # def class_name(options = {})
      #   return @class_name if @flags.blank?

      #   # Step through options and pull out any flags that we'll need to add
      #   # to our base class name
      #   class_names = [@class_name]
      #   options.each do |option, value|
      #     if @flags.key?(option)
      #       options.delete(option)
      #       class_names.push(strategy.flag_name_for(@class_name, flag: @flags[option])) if value
      #     end
      #   end

      #   class_names.join(" ")
      # end
    end
  end
end
